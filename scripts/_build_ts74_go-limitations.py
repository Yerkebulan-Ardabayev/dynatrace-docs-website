"""Build: ingest-from/technology-support/application-software/go/support/go-known-limitations.md"""
import sys, os
sys.path.insert(0, os.path.dirname(__file__))
from _zos_canon_l4if71 import build_one, qa_one

REL = "ingest-from/technology-support/application-software/go/support"
FNAME = "go-known-limitations.md"

TRANS = {
    "title: Known limitations for Go support": "title: Известные ограничения поддержки Go",
    "# Known limitations for Go support": "# Известные ограничения поддержки Go",
    "* 11-min read": "* Чтение: 11 мин",
    "* Updated on Mar 30, 2026": "* Обновлено 30 марта 2026 г.",
    "Before you start using Go application monitoring, make sure that you are aware of the known limitations.": "Перед началом использования мониторинга приложений Go необходимо ознакомиться с известными ограничениями.",
    "## Support limited to official, stable Go releases": "## Поддержка ограничена официальными стабильными релизами Go",
    "Go support is limited to:": "Поддержка Go ограничена:",
    "* Official, stable [Go releases](https://dt-url.net/go-releases) compiled with the Golang toolchain.": "* официальными стабильными [релизами Go](https://dt-url.net/go-releases), скомпилированными с помощью инструментария Golang;",
    "* Stable [Go releases](https://dt-url.net/go-releases) with [openssl-fips](https://dt-url.net/golang-fips) modifications to support the Federal Information Processing Standards (OneAgent version 1.295+).": "* стабильными [релизами Go](https://dt-url.net/go-releases) с модификациями [openssl-fips](https://dt-url.net/golang-fips) для поддержки стандартов FIPS (OneAgent версии 1.295+).",
    "OneAgent doesn't support binaries compiled using other toolchains like": "OneAgent не поддерживает исполняемые файлы, скомпилированные с помощью других инструментариев, например:",
    "* [gccgo toolchain](https://dt-url.net/gccgo-toolchain)": "* [инструментарий gccgo](https://dt-url.net/gccgo-toolchain)",
    "## Applications built with `-linkshared` option aren't supported": "## Приложения, собранные с параметром `-linkshared`, не поддерживаются",
    "Go supports dynamic linking of the Go standard library. This build mode is rarely used, and OneAgent won't inject into applications built this way.": "Go поддерживает динамическое связывание стандартной библиотеки Go. Этот режим сборки используется редко, и OneAgent не выполняет инъекцию в приложения, собранные таким образом.",
    "Example": "Пример",
    "Consider the following minimalistic Go application called `GoMinimal.go`:": "Рассмотрим следующее минималистичное Go-приложение с именем `GoMinimal.go`:",
    "OneAgent will reject the resulting application binary.": "OneAgent отклонит полученный исполняемый файл приложения.",
    "## Applications built with `-buildmode=pie` option and CGO disabled aren't supported": "## Приложения, собранные с параметром `-buildmode=pie` и отключённым CGO, не поддерживаются",
    "This restriction applies only to Linux systems.": "Это ограничение применяется только к системам Linux.",
    "Building the application with `-buildmode=pie` and `CGO_ENABLED=0` results in a dynamically linked application binary but without `libc` system library dependency, `libc` is required by OneAgent.": "Сборка приложения с `-buildmode=pie` и `CGO_ENABLED=0` создаёт динамически связанный исполняемый файл приложения, но без зависимости от системной библиотеки `libc`, которая необходима OneAgent.",
    "Example and workaround": "Пример и обходное решение",
    "Building the application with the following command results in a dynamically linked application binary that does not depend on `libc`, which is required by OneAgent:": "Сборка приложения следующей командой создаёт динамически связанный исполняемый файл приложения без зависимости от `libc`, которая необходима OneAgent:",
    "As a workaround, there are several options:": "В качестве обходного решения доступны несколько вариантов:",
    "* Remove `-buildmode=pie` option which results in a statically linked Go application (see [Go static monitoring](/managed/ingest-from/technology-support/application-software/go/configuration-and-analysis/enable-go-monitoring#go-static-monitoring \"Learn how you can enable Go monitoring in Dynatrace.\")).": "* Удалить параметр `-buildmode=pie`, что приведёт к созданию статически связанного приложения Go (см. [Статический мониторинг Go](/managed/ingest-from/technology-support/application-software/go/configuration-and-analysis/enable-go-monitoring#go-static-monitoring \"Узнайте, как включить мониторинг Go в Dynatrace.\")).",
    "* Use an external linker and do not disable CGO (`CGO_ENABLED=1` is the default).": "* Использовать внешний компоновщик и не отключать CGO (по умолчанию `CGO_ENABLED=1`).",
    "## Applications that load Go plugins aren't supported": "## Приложения, загружающие плагины Go, не поддерживаются",
    "A [Go plugin](https://dt-url.net/eae3riv) is a package compiled using the `-buildmode=plugin` build flag to produce a shared object file. This build mode is rarely used, and OneAgent will disable deep monitoring when an application actually loads a Go plugin.": "Плагин Go ([Go plugin](https://dt-url.net/eae3riv)), это пакет, скомпилированный с использованием флага сборки `-buildmode=plugin` для создания файла общего объекта. Этот режим сборки используется редко, и OneAgent отключает глубокий мониторинг, когда приложение фактически загружает плагин Go.",
    "## Vendored third-party packages aren't supported": "## Сторонние пакеты с вендорингом не поддерживаются",
    "[Go vendoring](https://dt-url.net/ubg3r7o) is used to include local copies of external dependencies in the project repository. This approach was used to pin versions of third-party packages before [Go module](https://dt-url.net/nci3rry) support was added.": "[Go vendoring](https://dt-url.net/ubg3r7o) используется для включения локальных копий внешних зависимостей в репозиторий проекта. Этот подход применялся для фиксации версий сторонних пакетов до добавления поддержки [Go module](https://dt-url.net/nci3rry).",
    "OneAgent will not monitor vendored packages. For example, gRPC services are supported only if you use Go modules or if you import [go-grpc](https://dt-url.net/k6k3r67) directly without using a dependency management system.": "OneAgent не выполняет мониторинг пакетов с вендорингом. Например, сервисы gRPC поддерживаются только при использовании Go modules или при прямом импорте [go-grpc](https://dt-url.net/k6k3r67) без применения системы управления зависимостями.",
    "## Possible limitations without a symbol table": "## Возможные ограничения при отсутствии таблицы символов",
    "By default, Go generates application binaries that contain a symbol table.": "По умолчанию Go создаёт исполняемые файлы приложений, содержащие таблицу символов.",
    "Currently there are no known limitations for stripped binaries in Dynatrace. However, we can't guarantee that all current features will continue to work in future Go versions or that all features added later will be supported in stripped binaries.": "На данный момент в Dynatrace нет известных ограничений для бинарных файлов со stripped-символами. Однако нельзя гарантировать, что все текущие функции продолжат работать в будущих версиях Go или что все добавленные позднее функции будут поддерживаться в stripped-бинарных файлах.",
    "We therefore recommend that you build Go binaries that contain a symbol table and avoid the use of command line parameters or external tools that might suppress it.": "Рекомендуется собирать бинарные файлы Go, содержащие таблицу символов, и избегать использования параметров командной строки или внешних инструментов, которые могут её удалить.",
    "* Don't use the external tool `strip` (`strip <Go binary>`).": "* Не используйте внешний инструмент `strip` (`strip <Go binary>`).",
    "* Don't compile with `go build -ldflags=\"-s\"`. The `-s` flag strips away the symbol table.": "* Не компилируйте с `go build -ldflags=\"-s\"`. Флаг `-s` удаляет таблицу символов.",
    "* Don't run `go run <application>`. This rarely used command builds and runs applications on the fly. Because the output application file is temporary (the file is deleted automatically after the app termination), the application binary contains no symbol table.": "* Не запускайте `go run <application>`. Эта редко используемая команда собирает и запускает приложения на лету. Поскольку выходной файл приложения является временным (файл удаляется автоматически после завершения работы приложения), исполняемый файл приложения не содержит таблицу символов.",
    "In Dynatrace, monitoring of stripped binaries is enabled by default. However, you can disable the feature for the tenant by turning off **Go stripped binaries** on the [**OneAgent features**](/managed/ingest-from/dynatrace-oneagent/oneagent-features \"Manage OneAgent features globally and per process group.\") settings page.": "В Dynatrace мониторинг stripped-бинарных файлов включён по умолчанию. При необходимости функцию можно отключить для тенанта, выключив **Go stripped binaries** на странице настроек [**OneAgent features**](/managed/ingest-from/dynatrace-oneagent/oneagent-features \"Управление функциями OneAgent глобально и на уровне группы процессов.\").",
    "Non-stripped binaries on AArch64 are supported starting with OneAgent version 1.323. OneAgent version 1.327 adds support for stripped binaries on AArch64.": "Non-stripped бинарные файлы на AArch64 поддерживаются начиная с OneAgent версии 1.323. Поддержка stripped бинарных файлов на AArch64 добавлена в OneAgent версии 1.327.",
    "## Applications built with race detector enabled aren't supported": "## Приложения, собранные с включённым детектором гонок, не поддерживаются",
    "An application built with `-race` flag contains a built-in [data race detector](https://golang.org/doc/articles/race_detector).": "Приложение, собранное с флагом `-race`, содержит встроенный [детектор гонок данных](https://golang.org/doc/articles/race_detector).",
    "This build mode is mostly used in a development environment and OneAgent won't inject into applications built this way.": "Этот режим сборки в основном используется в среде разработки, и OneAgent не выполняет инъекцию в приложения, собранные таким образом.",
    "## Creation stack profiling of OS threads is disabled": "## Профилирование стека создания потоков ОС отключено",
    "OneAgent does not support the [predefined `threadcreate` profile](https://golang.org/doc/diagnostics#profiling). Thread creation profiling results of Go applications monitored by OneAgent will contain empty stack traces only.": "OneAgent не поддерживает [предопределённый профиль `threadcreate`](https://golang.org/doc/diagnostics#profiling). Результаты профилирования создания потоков приложений Go, отслеживаемых OneAgent, будут содержать только пустые трассы стека.",
    "## Support for statically linked binaries (Linux only)": "## Поддержка статически связанных бинарных файлов (только Linux)",
    "OneAgent version 1.203+": "OneAgent версии 1.203+",
    "Prior to OneAgent version 1.203, statically linked binaries are not supported. See [below](#no-static-monitoring) for details.": "До OneAgent версии 1.203 статически связанные бинарные файлы не поддерживаются. Подробности в разделе [ниже](#no-static-monitoring).",
    "After you [enable Go static monitoring](/managed/ingest-from/technology-support/application-software/go/configuration-and-analysis/enable-go-monitoring#go-static-monitoring \"Learn how you can enable Go monitoring in Dynatrace.\"), automatic injection for statically linked Go binaries is supported by OneAgent if": "После [включения статического мониторинга Go](/managed/ingest-from/technology-support/application-software/go/configuration-and-analysis/enable-go-monitoring#go-static-monitoring \"Узнайте, как включить мониторинг Go в Dynatrace.\") OneAgent поддерживает автоматическую инъекцию для статически связанных бинарных файлов Go при выполнении следующих условий:",
    "* The parent process is dynamically linked. This also applies to applications running as a container payload.": "* Родительский процесс является динамически связанным. Это также распространяется на приложения, работающие в качестве полезной нагрузки контейнера.",
    "In this example, the parent process is a `/bin/sh` shell that starts a statically linked Go binary. The following code launches the `/bin/sh` shell and executes the provided command.": "В данном примере родительским процессом является оболочка `/bin/sh`, запускающая статически связанный исполняемый файл Go. Следующий код запускает оболочку `/bin/sh` и выполняет указанную команду.",
    "You can use the `file` command to verify if an application is dynamically or statically linked.": "Для проверки того, является ли приложение динамически или статически связанным, можно использовать команду `file`.",
    "* [Kubernetes Classic full-stack injection](/managed/ingest-from/setup-on-k8s/how-it-works#classic \"In-depth description on how the deployment on Kubernetes works.\") The statically linked Go binary is running as a Docker container entrypoint.": "* [Kubernetes Classic full-stack injection](/managed/ingest-from/setup-on-k8s/how-it-works#classic \"Подробное описание развёртывания в Kubernetes.\"). Статически связанный исполняемый файл Go запущен как точка входа контейнера Docker.",
    "If your setup is not supported by automatic injection, we recommend calling the static Go application via a shell (`/bin/sh -c '/StaticGoMinimal <optional app arguments>'`).": "Если автоматическая инъекция не поддерживается для вашей конфигурации, рекомендуется вызывать статическое приложение Go через оболочку (`/bin/sh -c '/StaticGoMinimal <optional app arguments>'`).",
    "### Cloud-native full-stack injection": "### Cloud-native full-stack injection",
    "Automatic injection of statically linked Go applications running as container entrypoints is not supported when using the [cloud-native full-stack injection](/managed/ingest-from/setup-on-k8s/how-it-works#cloud-native \"In-depth description on how the deployment on Kubernetes works.\") deployment option in Kubernetes.": "Автоматическая инъекция статически связанных приложений Go, работающих в качестве точек входа контейнеров, не поддерживается при использовании варианта развёртывания [cloud-native full-stack injection](/managed/ingest-from/setup-on-k8s/how-it-works#cloud-native \"Подробное описание развёртывания в Kubernetes.\") в Kubernetes.",
    "For example, in a cloud-native full-stack injection deployment, the following code starts an unsupported statically linked Go application.": "Например, при развёртывании с cloud-native full-stack injection следующий код запускает неподдерживаемое статически связанное приложение Go.",
    "To overcome this limitation, you can change the container entrypoint from a statically linked Go application to a dynamically linked application such as a shell or `init`.": "Для устранения этого ограничения можно изменить точку входа контейнера со статически связанного приложения Go на динамически связанное приложение, например оболочку или `init`.",
    "Workaround when a shell is available": "Обходное решение при наличии оболочки",
    "Applies to container images that already contain a dynamically linked shell binary.": "Применяется к образам контейнеров, которые уже содержат динамически связанный исполняемый файл оболочки.",
    "By changing the container entryproint from a statically linked Go application to a dynamically linked shell, we obtain the following code that launches `/bin/sh` and executes the `/StaticGoMinimal` command.": "Изменив точку входа контейнера со статически связанного приложения Go на динамически связанную оболочку, получим следующий код, который запускает `/bin/sh` и выполняет команду `/StaticGoMinimal`.",
    "Workaround when a shell is not available": "Обходное решение при отсутствии оболочки",
    "Applies to container images where a shell is not available.": "Применяется к образам контейнеров, в которых оболочка недоступна.",
    "When the container image does not contain a shell, for example in [distroless images](https://dt-url.net/up23q6j), another option is to use a minimal `init` binary like [tini](https://dt-url.net/he03qck).": "Если образ контейнера не содержит оболочку, например в случае [distroless images](https://dt-url.net/up23q6j), можно использовать минимальный бинарный файл `init`, например [tini](https://dt-url.net/he03qck).",
    "By adding tini and changing the container entryproint, we obtain the following code that executes `/StaticGoMinimal` while preserving proper signal forwarding.": "Добавив tini и изменив точку входа контейнера, получим следующий код, который выполняет `/StaticGoMinimal` с сохранением корректной пересылки сигналов.",
    "Note the parser directive specifying Dockerfile syntax version 1, which is required for using the `--checksum` and `--chmod` options with `ADD`. Without it, the image would have to provide the `chmod` binary so `tini` can be made executable.": "Обратите внимание на директиву парсера, задающую синтаксис Dockerfile версии 1, которая необходима для использования параметров `--checksum` и `--chmod` с командой `ADD`. Без неё образ должен предоставлять бинарный файл `chmod`, чтобы сделать `tini` исполняемым.",
    "There are several variants of tini available for glibc and musl, as well as amd64 and aarch64 architectures.": "Для glibc и musl, а также архитектур amd64 и aarch64 доступны несколько вариантов tini.",
    "### Limitations": "### Ограничения",
    "* Static Go applications that use cgo are not supported.": "* Статические приложения Go, использующие cgo, не поддерживаются.",
    "OneAgent rejects monitoring of static Go binaries that use [cgo](https://go.dev/blog/cgo) and, therefore, have a static dependency on the C system library libc. This is because the statically linked version of libc might conflict with the one used by OneAgent.": "OneAgent отклоняет мониторинг статических бинарных файлов Go, использующих [cgo](https://go.dev/blog/cgo) и, следовательно, имеющих статическую зависимость от системной библиотеки C libc. Это связано с тем, что статически связанная версия libc может конфликтовать с версией, используемой OneAgent.",
    "Workaround": "Обходное решение",
    "To overcome this limitation, you can build the Go application as a [dynamically linked executable](/managed/ingest-from/technology-support/application-software/go/support/go-known-limitations#go-dynamic-linking \"Learn the limitations for Go support and their workarounds.\") that dynamically links to libc. This will ensure that both the Go application and OneAgent use the same version of libc, which is the one available on the host.": "Для устранения этого ограничения можно собрать приложение Go как [динамически связанный исполняемый файл](/managed/ingest-from/technology-support/application-software/go/support/go-known-limitations#go-dynamic-linking \"Ограничения поддержки Go и их обходные решения.\"), который динамически связывается с libc. Это гарантирует, что приложение Go и OneAgent используют одну и ту же версию libc, доступную на хосте.",
    "* OneAgent version 1.337+ Static Go monitoring requires either a Linux kernel that provides the": "* OneAgent версии 1.337+ для статического мониторинга Go требуется либо ядро Linux, предоставляющее",
    "`memfd_create` syscall (added in Linux 3.17, but backported to earlier versions on some distributions), or the": "системный вызов `memfd_create` (добавлен в Linux 3.17, но перенесён в более ранние версии в некоторых дистрибутивах), либо",
    "`SYS_PTRACE` capability (see next point).": "возможность `SYS_PTRACE` (см. следующий пункт).",
    "* OneAgent version 1.335 and earlier Static Go monitoring requires the `SYS_PTRACE` capability.": "* OneAgent версии 1.335 и более ранних для статического мониторинга Go требуется возможность `SYS_PTRACE`.",
    "The `SYS_PTRACE` capability is enabled by default for Docker 19.03.0+ and Linux Kernel 4.8+. It allows system calls between processes running in a container, which is a requirement for Go static monitoring.": "Возможность `SYS_PTRACE` по умолчанию включена для Docker 19.03.0+ и ядра Linux 4.8+. Она разрешает системные вызовы между процессами, работающими в контейнере, что является требованием для статического мониторинга Go.",
    "Workaround and example": "Обходное решение и пример",
    "You can overcome this limitation for Docker versions earlier than 19.03.0 or Linux Kernel versions earlier than 4.8 by running the container with the `SYS_PTRACE` capability as shown below.": "Для версий Docker ранее 19.03.0 или ядра Linux ранее 4.8 можно устранить это ограничение, запустив контейнер с возможностью `SYS_PTRACE`, как показано ниже.",
    "* Docker images that don't provide a C system library are not supported.": "* Образы Docker, не предоставляющие системную библиотеку C, не поддерживаются.",
    "OneAgent requires a C system library to be available on the monitored host.": "OneAgent требует наличия системной библиотеки C на отслеживаемом хосте.",
    "To overcome this limitation, you can change the base image of a container to one that provides a C system library.": "Для устранения этого ограничения можно изменить базовый образ контейнера на тот, который предоставляет системную библиотеку C.",
    "An example of a Docker image that doesn't provide a C system library is the [scratch image](https://dt-url.net/6083rfq).": "Пример образа Docker, не предоставляющего системную библиотеку C: [scratch image](https://dt-url.net/6083rfq).",
    "Examples of images that provide a C system library are the [Alpine image](https://dt-url.net/ksa3rnj) or various [distroless images](https://dt-url.net/up23q6j).": "Примеры образов, предоставляющих системную библиотеку C: [Alpine image](https://dt-url.net/ksa3rnj) и различные [distroless images](https://dt-url.net/up23q6j).",
    "### Side effects": "### Побочные эффекты",
    "The file `proc/<pId>/exe` refers to an executable named `oneagentdynamizer` instead of the Go application binary, it is contained in the [proc](https://dt-url.net/94c3rfn) pseudo-filesystem that provides an interface to kernel data structures of running processes. This may cause system tools like `ps` or `top` to display `oneagentdynamizer` instead of the Go binary name in their output.": "Файл `proc/<pId>/exe` ссылается на исполняемый файл с именем `oneagentdynamizer` вместо бинарного файла приложения Go. Он содержится в псевдофайловой системе [proc](https://dt-url.net/94c3rfn), предоставляющей интерфейс к структурам данных ядра для запущенных процессов. Это может приводить к тому, что системные инструменты, такие как `ps` или `top`, отображают `oneagentdynamizer` вместо имени бинарного файла Go.",
    "### OneAgent versions without support for static monitoring": "### Версии OneAgent без поддержки статического мониторинга",
    "OneAgent version 1.201 and earlier": "OneAgent версии 1.201 и более ранних",
    "Prior to OneAgent version 1.203, statically linked binaries are not supported and dynamic linking is necessary for injection on Linux. There is no such limitation on Windows.": "До OneAgent версии 1.203 статически связанные бинарные файлы не поддерживаются, и для инъекции в Linux необходимо динамическое связывание. В Windows такого ограничения нет.",
    "Dynamic linking is automatically applied when the application uses certain standard runtime library packages, for example, `net/http`.": "Динамическое связывание применяется автоматически, когда приложение использует определённые пакеты стандартной библиотеки времени выполнения, например `net/http`.",
    "In all other cases, you can enforce dynamic linking through the `-ldflags=\"-linkmode=external\"` command line option. Note that disabling cgo, for example, using `CGO_ENABLED=0`, is not supported, and OneAgent will reject the resulting application binary.": "Во всех остальных случаях динамическое связывание можно принудительно задать с помощью параметра командной строки `-ldflags=\"-linkmode=external\"`. Обратите внимание, что отключение cgo, например с помощью `CGO_ENABLED=0`, не поддерживается, и OneAgent отклонит полученный исполняемый файл приложения.",
    "Building the application results in a statically linked application binary:": "Сборка приложения создаёт статически связанный исполняемый файл приложения:",
    "You can enforce dynamic linking with `-ldflags=\"-linkmode=external\"`:": "Динамическое связывание можно принудительно задать с помощью `-ldflags=\"-linkmode=external\"`:",
    "## Support for musl libc": "## Поддержка musl libc",
    "The musl libc library is a drop-in replacement for the glibc library. Dynatrace supports musl-based Go applications, such as those built on Alpine Linux.": "Библиотека musl libc является прямой заменой библиотеки glibc. Dynatrace поддерживает приложения Go на основе musl, например собранные на Alpine Linux.",
    "There is one additional requirement for building a dynamically linked application binary. You should use the [Go toolchain for alpine (golang:<version>-alpine)](https://dt-url.net/3gm3rwp) and add `-ldflags=\"-linkmode=external\"` (or add `-linkmode=external` to an existing `-ldflags`) to the build command line to enforce usage of the system linker. This is not required for statically linked Go applications watched by [Go static monitoring](/managed/ingest-from/technology-support/application-software/go/configuration-and-analysis/enable-go-monitoring#go-static-monitoring \"Learn how you can enable Go monitoring in Dynatrace.\").": "Для сборки динамически связанного исполняемого файла приложения есть одно дополнительное требование. Необходимо использовать [инструментарий Go для alpine (golang:<version>-alpine)](https://dt-url.net/3gm3rwp) и добавить `-ldflags=\"-linkmode=external\"` (или добавить `-linkmode=external` к существующему `-ldflags`) в командную строку сборки для принудительного использования системного компоновщика. Это не требуется для статически связанных приложений Go, отслеживаемых [статическим мониторингом Go](/managed/ingest-from/technology-support/application-software/go/configuration-and-analysis/enable-go-monitoring#go-static-monitoring \"Узнайте, как включить мониторинг Go в Dynatrace.\").",
    "Details": "Подробности",
    "While musl libc does closely mimic the glibc functionalities, there are subtle behavioral differences between the two. Moreover, Go doesn't officially support the musl-based Go toolchain, which means Go toolchain binaries can't be downloaded from the [golang.org](https://dt-url.net/go) website.": "Хотя musl libc точно имитирует функции glibc, между ними есть тонкие поведенческие различия. Кроме того, Go официально не поддерживает инструментарий Go на основе musl, а значит, бинарные файлы инструментария Go нельзя загрузить с веб-сайта [golang.org](https://dt-url.net/go).",
    "In addition, there is a [serious issue](https://dt-url.net/6vq3rim) with how Go uses musl libc. This limits the extent to which Dynatrace can support musl-based applications. The Go toolchain includes an internal linker that generates musl-based application binaries that don't correctly initialize musl libc at application startup. This issue prevents Dynatrace from monitoring these applications. In such a case, the following message is displayed on the relevant application process page:": "Кроме того, существует [серьёзная проблема](https://dt-url.net/6vq3rim) с тем, как Go использует musl libc. Это ограничивает возможности Dynatrace по поддержке приложений на основе musl. Инструментарий Go включает внутренний компоновщик, который создаёт бинарные файлы приложений на основе musl, не инициализирующие musl libc правильно при запуске приложения. Эта проблема не позволяет Dynatrace отслеживать такие приложения. В этом случае на странице соответствующего процесса приложения отображается следующее сообщение:",
    "**Activation of deep monitoring was unsuccessful, Monitoring of Go musl binaries built with Go internal linker is not supported**": "**Активация глубокого мониторинга завершилась неудачно. Мониторинг бинарных файлов Go musl, собранных с внутренним компоновщиком Go, не поддерживается**",
    "If you use the system linker to generate the application binary, it adds startup code that correctly initializes shared objects. Also, adding `-ldflags=\"-linkmode=external\"` to the build command line enforces usage of the system linker. The resulting binary will execute with a correctly initialized libc, allowing Dynatrace to monitor such an application.": "При использовании системного компоновщика для создания бинарного файла приложения он добавляет стартовый код, правильно инициализирующий общие объекты. Добавление `-ldflags=\"-linkmode=external\"` в командную строку сборки принудительно задаёт использование системного компоновщика. Полученный бинарный файл будет выполняться с правильно инициализированным libc, что позволяет Dynatrace отслеживать такое приложение.",
    "The following multi-stage docker file yields a valid dynamically linked Go musl binary in stage 1 and runs the application in stage 2.": "Следующий многоэтапный Dockerfile создаёт допустимый динамически связанный бинарный файл Go musl на этапе 1 и запускает приложение на этапе 2.",
    "Build the container and run the application:": "Соберите контейнер и запустите приложение:",
    "Additionally, there is an issue with the musl malloc implementation on machines that have 64 or more CPUs, which can result in high CPU overhead. This can be resolved by either": "Кроме того, существует проблема с реализацией musl malloc на машинах с 64 и более ЦП, которая может привести к высокой нагрузке на ЦП. Это можно решить следующими способами:",
    "* Using a base image that uses glibc instead.": "* использование базового образа на основе glibc;",
    "* Preloading an alternative malloc implementation, such as [tcmalloc](https://dt-url.net/xz03zxt) or [jemalloc](https://dt-url.net/9523zi0), as described below.": "* предварительная загрузка альтернативной реализации malloc, например [tcmalloc](https://dt-url.net/xz03zxt) или [jemalloc](https://dt-url.net/9523zi0), как описано ниже.",
    "Preloading tcmalloc": "Предварительная загрузка tcmalloc",
    "To preload tcmalloc, take the Dockerfile from the example above and adjust Stage 2 as shown in the code block below.": "Для предварительной загрузки tcmalloc возьмите Dockerfile из приведённого выше примера и скорректируйте этап 2, как показано в блоке кода ниже.",
    "Preloading jemalloc": "Предварительная загрузка jemalloc",
    "To preload jemalloc, take the Dockerfile from the example above and adjust Stage 2 as shown in the code block below.": "Для предварительной загрузки jemalloc возьмите Dockerfile из приведённого выше примера и скорректируйте этап 2, как показано в блоке кода ниже.",
    "Consider the following Go application called `main.go`:": "Рассмотрим следующее приложение Go под названием `main.go`:",
}

PASS = {
    "# Known limitations for Go support",
}

build_one(REL, FNAME, TRANS, PASS)
print("build OK")
qa_one(REL, FNAME)
