---
title: Monitored entities API - контекст безопасности
source: https://www.dynatrace.com/docs/dynatrace-api/environment-api/entity-v2/security-context
scraped: 2026-03-06T21:29:16.110902
---

# API отслеживаемых сущностей — контекст безопасности


* Устарело

Этот API устарел. Используйте схему Management zones (`builtin:management-zones`) из Settings API.

Создание или удаление контекста безопасности для отслеживаемых сущностей.

Соответствующим сущностям будет назначена зона управления, если указанный контекст безопасности совпадает с именем уже существующей зоны управления. Этот эндпоинт не создаёт новую зону управления, если зоны с указанным именем не существует.

Правила зон управления не будут применяться к сущностям с установленным контекстом безопасности. Чтобы снова применять их, необходимо удалить контекст безопасности.

Подробнее о контексте безопасности см. Предоставление доступа к сущностям с помощью контекста безопасности.

## Создание контекста безопасности

Запрос использует и возвращает полезную нагрузку `application/json`.

|  |  |  |
| --- | --- | --- |
| POST | Managed | `https://{your-environment-id}.live.dynatrace.com/api/v2/entities/securityContext` |
| POST | Environment ActiveGateCluster ActiveGate | `https://{your-activegate-domain}:9999/e/{your-environment-id}/api/v2/entities/securityContext` |

## Аутентификация

Для выполнения этого запроса необходим токен доступа с областью `settings.write`.

Чтобы узнать, как получить и использовать его, см. Токены и аутентификация.

### Параметры

| Параметр | Тип | Описание | Расположение | Обязательный |
| --- | --- | --- | --- | --- |
| entitySelector | string | Определяет область сущностей для установки контекста безопасности. Учитываются только сущности, которым можно назначить зоны управления.  Необходимо указать один из следующих критериев:  * Тип сущности: `type("TYPE")` * Идентификатор сущности Dynatrace: `entityId("id")`. Можно указать несколько ID через запятую (`entityId("id-1","id-2")`). Все запрашиваемые сущности должны быть одного типа.  Можно добавить один или несколько из следующих критериев. Значения чувствительны к регистру, и используется оператор `EQUALS`, если не указано иное.  * Тег: `tag("value")`. Теги в форматах `[context]key:value`, `key:value` и `value` определяются и разбираются автоматически. Двоеточия (`:`) в составе ключа или значения должны быть экранированы обратной косой чертой (`\`). Иначе они будут интерпретированы как разделитель между ключом и значением. Все значения тегов чувствительны к регистру. * ID зоны управления: `mzId(123)` * Имя зоны управления: `mzName("value")` * Имя сущности: + `entityName.equals`: выполняет нечувствительный к регистру запрос `EQUALS`.   + `entityName.startsWith`: изменяет оператор на `BEGINS WITH`.   + `entityName.in`: позволяет указать несколько значений. Применяется оператор `EQUALS`.   + `caseSensitive(entityName.equals("value"))`: принимает любой критерий имени сущности в качестве аргумента и делает значение чувствительным к регистру. * Состояние здоровья (HEALTHY,UNHEALTHY): `healthState("HEALTHY")` * Метка времени первого обнаружения: `firstSeenTms.<operator>(now-3h)`. Используйте любой формат метки времени из параметров **from**/**to**.   Доступны следующие операторы: + `lte`: раньше или в указанное время   + `lt`: раньше указанного времени   + `gte`: позже или в указанное время   + `gt`: позже указанного времени * Атрибут сущности: `<attribute>("value1","value2")` и `<attribute>.exists()`. Для получения списка доступных атрибутов выполните запрос [GET entity type](https://dt-url.net/2ka3ivt) и проверьте поле **properties** ответа. * Связи: `fromRelationships.<relationshipName>()` и `toRelationships.<relationshipName>()`. Этот критерий принимает селектор сущностей в качестве атрибута. Для получения списка доступных связей выполните запрос [GET entity type](https://dt-url.net/2ka3ivt) и проверьте поля **fromRelationships** и **toRelationships**. * Отрицание: `not(<criterion>)`. Инвертирует любой критерий, кроме **type**.  Подробнее см. [Селектор сущностей](https://dt-url.net/apientityselector) в документации Dynatrace.  Для указания нескольких критериев разделите их запятой (`,`). Например, `type("HOST"),healthState("HEALTHY")`. В ответ включаются только результаты, соответствующие **всем** критериям.  Максимальная длина строки — 2000 символов. | query | Обязательный |
| from | string | Начало запрашиваемого временного интервала.  Можно использовать один из следующих форматов:  * Метка времени в миллисекундах UTC. * Человекочитаемый формат `2021-01-25T05:57:01.123+01:00`. Если часовой пояс не указан, используется UTC. Вместо `T` можно использовать пробел. Секунды и дробные части секунды необязательны. * Относительный временной интервал от текущего момента. Формат: `now-NU/A`, где `N` — количество времени, `U` — единица времени, `A` — выравнивание. Выравнивание округляет все меньшие значения до ближайшего нуля в прошлом. Например, `now-1y/w` — один год назад с выравниванием по неделе.   Можно указать относительный интервал без выравнивания: `now-NU`.   Поддерживаемые единицы времени: + `m`: минуты   + `h`: часы   + `d`: дни   + `w`: недели   + `M`: месяцы   + `y`: годы  Если не указано, используется относительный интервал в три дня (`now-3d`). | query | Необязательный |
| to | string | Конец запрашиваемого временного интервала.  Можно использовать один из следующих форматов:  * Метка времени в миллисекундах UTC. * Человекочитаемый формат `2021-01-25T05:57:01.123+01:00`. Если часовой пояс не указан, используется UTC. Вместо `T` можно использовать пробел. Секунды и дробные части секунды необязательны. * Относительный временной интервал от текущего момента. Формат: `now-NU/A`, где `N` — количество времени, `U` — единица времени, `A` — выравнивание. Выравнивание округляет все меньшие значения до ближайшего нуля в прошлом. Например, `now-1y/w` — один год назад с выравниванием по неделе.   Можно указать относительный интервал без выравнивания: `now-NU`.   Поддерживаемые единицы времени: + `m`: минуты   + `h`: часы   + `d`: дни   + `w`: недели   + `M`: месяцы   + `y`: годы  Если не указано, используется текущая метка времени. | query | Необязательный |
| body | [SecurityContextDtoImpl](#openapi-definition-SecurityContextDtoImpl) | JSON-тело запроса. Содержит контекст безопасности для установки соответствующим сущностям. | body | Необязательный |

### Объекты тела запроса

#### Объект `SecurityContextDtoImpl`

| Элемент | Тип | Описание | Обязательный |
| --- | --- | --- | --- |
| securityContext | string[] | Контекст безопасности, который будет установлен для соответствующих сущностей. Если существует зона управления с таким именем, она будет назначена всем соответствующим сущностям, переопределяя все автоматические правила зон управления. | Необязательный |

### JSON-модель тела запроса

Это модель тела запроса, показывающая возможные элементы. Она должна быть адаптирована для использования в реальном запросе.

```
{


"securityContext": [


"string"


]


}
```

### Ответ

### Коды ответа

| Код | Тип | Описание |
| --- | --- | --- |
| **200** | [SecurityContextResultDto](#openapi-definition-SecurityContextResultDto) | Успех |
| **4XX** | [ErrorEnvelope](#openapi-definition-ErrorEnvelope) | Ошибка на стороне клиента. |
| **5XX** | [ErrorEnvelope](#openapi-definition-ErrorEnvelope) | Ошибка на стороне сервера. |

### Объекты тела ответа

#### Объект `SecurityContextResultDto`

Полезная нагрузка ответа, содержащая результат применения контекста безопасности.

| Элемент | Тип | Описание |
| --- | --- | --- |
| entityIds | string[] | Идентификаторы сущностей, которые соответствовали селектору сущностей и теперь имеют установленный контекст безопасности. |
| managementZoneIds | integer[] | Идентификаторы зон управления, применённых к идентификаторам сущностей, если контекст безопасности совпал с именем существующей зоны управления; в противном случае — null. |

#### Объект `ErrorEnvelope`

| Элемент | Тип | Описание |
| --- | --- | --- |
| error | [Error](#openapi-definition-Error) | - |

#### Объект `Error`

| Элемент | Тип | Описание |
| --- | --- | --- |
| code | integer | Код состояния HTTP |
| constraintViolations | [ConstraintViolation[]](#openapi-definition-ConstraintViolation) | Список нарушений ограничений |
| message | string | Сообщение об ошибке |

#### Объект `ConstraintViolation`

Список нарушений ограничений

| Элемент | Тип | Описание |
| --- | --- | --- |
| location | string | - |
| message | string | - |
| parameterLocation | string | - Элемент может содержать следующие значения: * `HEADER` * `PATH` * `PAYLOAD_BODY` * `QUERY` |
| path | string | - |

### JSON-модели тела ответа

```
{


"entityIds": [


"string"


],


"managementZoneIds": [


1


]


}
```

```
{


"error": {


"code": 1,


"constraintViolations": [


{


"location": "string",


"message": "string",


"parameterLocation": "HEADER",


"path": "string"


}


],


"message": "string"


}


}
```

## Удаление контекста безопасности

|  |  |  |
| --- | --- | --- |
| DELETE | Managed | `https://{your-environment-id}.live.dynatrace.com/api/v2/entities/securityContext` |
| DELETE | Environment ActiveGateCluster ActiveGate | `https://{your-activegate-domain}:9999/e/{your-environment-id}/api/v2/entities/securityContext` |

## Аутентификация

Для выполнения этого запроса необходим токен доступа с областью `settings.write`.

Чтобы узнать, как получить и использовать его, см. Токены и аутентификация.

### Параметры

| Параметр | Тип | Описание | Расположение | Обязательный |
| --- | --- | --- | --- | --- |
| entitySelector | string | Определяет область сущностей для установки контекста безопасности. Учитываются только сущности, которым можно назначить зоны управления.  Необходимо указать один из следующих критериев:  * Тип сущности: `type("TYPE")` * Идентификатор сущности Dynatrace: `entityId("id")`. Можно указать несколько ID через запятую (`entityId("id-1","id-2")`). Все запрашиваемые сущности должны быть одного типа.  Можно добавить один или несколько из следующих критериев. Значения чувствительны к регистру, и используется оператор `EQUALS`, если не указано иное.  * Тег: `tag("value")`. Теги в форматах `[context]key:value`, `key:value` и `value` определяются и разбираются автоматически. Двоеточия (`:`) в составе ключа или значения должны быть экранированы обратной косой чертой (`\`). Иначе они будут интерпретированы как разделитель между ключом и значением. Все значения тегов чувствительны к регистру. * ID зоны управления: `mzId(123)` * Имя зоны управления: `mzName("value")` * Имя сущности: + `entityName.equals`: выполняет нечувствительный к регистру запрос `EQUALS`.   + `entityName.startsWith`: изменяет оператор на `BEGINS WITH`.   + `entityName.in`: позволяет указать несколько значений. Применяется оператор `EQUALS`.   + `caseSensitive(entityName.equals("value"))`: принимает любой критерий имени сущности в качестве аргумента и делает значение чувствительным к регистру. * Состояние здоровья (HEALTHY,UNHEALTHY): `healthState("HEALTHY")` * Метка времени первого обнаружения: `firstSeenTms.<operator>(now-3h)`. Используйте любой формат метки времени из параметров **from**/**to**.   Доступны следующие операторы: + `lte`: раньше или в указанное время   + `lt`: раньше указанного времени   + `gte`: позже или в указанное время   + `gt`: позже указанного времени * Атрибут сущности: `<attribute>("value1","value2")` и `<attribute>.exists()`. Для получения списка доступных атрибутов выполните запрос [GET entity type](https://dt-url.net/2ka3ivt) и проверьте поле **properties** ответа. * Связи: `fromRelationships.<relationshipName>()` и `toRelationships.<relationshipName>()`. Этот критерий принимает селектор сущностей в качестве атрибута. Для получения списка доступных связей выполните запрос [GET entity type](https://dt-url.net/2ka3ivt) и проверьте поля **fromRelationships** и **toRelationships**. * Отрицание: `not(<criterion>)`. Инвертирует любой критерий, кроме **type**.  Подробнее см. [Селектор сущностей](https://dt-url.net/apientityselector) в документации Dynatrace.  Для указания нескольких критериев разделите их запятой (`,`). Например, `type("HOST"),healthState("HEALTHY")`. В ответ включаются только результаты, соответствующие **всем** критериям.  Максимальная длина строки — 2000 символов. | query | Обязательный |
| from | string | Начало запрашиваемого временного интервала.  Можно использовать один из следующих форматов:  * Метка времени в миллисекундах UTC. * Человекочитаемый формат `2021-01-25T05:57:01.123+01:00`. Если часовой пояс не указан, используется UTC. Вместо `T` можно использовать пробел. Секунды и дробные части секунды необязательны. * Относительный временной интервал от текущего момента. Формат: `now-NU/A`, где `N` — количество времени, `U` — единица времени, `A` — выравнивание. Выравнивание округляет все меньшие значения до ближайшего нуля в прошлом. Например, `now-1y/w` — один год назад с выравниванием по неделе.   Можно указать относительный интервал без выравнивания: `now-NU`.   Поддерживаемые единицы времени: + `m`: минуты   + `h`: часы   + `d`: дни   + `w`: недели   + `M`: месяцы   + `y`: годы  Если не указано, используется относительный интервал в три дня (`now-3d`). | query | Необязательный |
| to | string | Конец запрашиваемого временного интервала.  Можно использовать один из следующих форматов:  * Метка времени в миллисекундах UTC. * Человекочитаемый формат `2021-01-25T05:57:01.123+01:00`. Если часовой пояс не указан, используется UTC. Вместо `T` можно использовать пробел. Секунды и дробные части секунды необязательны. * Относительный временной интервал от текущего момента. Формат: `now-NU/A`, где `N` — количество времени, `U` — единица времени, `A` — выравнивание. Выравнивание округляет все меньшие значения до ближайшего нуля в прошлом. Например, `now-1y/w` — один год назад с выравниванием по неделе.   Можно указать относительный интервал без выравнивания: `now-NU`.   Поддерживаемые единицы времени: + `m`: минуты   + `h`: часы   + `d`: дни   + `w`: недели   + `M`: месяцы   + `y`: годы  Если не указано, используется текущая метка времени. | query | Необязательный |

### Ответ

### Коды ответа

| Код | Тип | Описание |
| --- | --- | --- |
| **200** | [SecurityContextResultDto](#openapi-definition-SecurityContextResultDto) | Успех |
| **4XX** | [ErrorEnvelope](#openapi-definition-ErrorEnvelope) | Ошибка на стороне клиента. |
| **5XX** | [ErrorEnvelope](#openapi-definition-ErrorEnvelope) | Ошибка на стороне сервера. |

### Объекты тела ответа

#### Объект `SecurityContextResultDto`

Полезная нагрузка ответа, содержащая результат применения контекста безопасности.

| Элемент | Тип | Описание |
| --- | --- | --- |
| entityIds | string[] | Идентификаторы сущностей, которые соответствовали селектору сущностей и теперь имеют установленный контекст безопасности. |
| managementZoneIds | integer[] | Идентификаторы зон управления, применённых к идентификаторам сущностей, если контекст безопасности совпал с именем существующей зоны управления; в противном случае — null. |

#### Объект `ErrorEnvelope`

| Элемент | Тип | Описание |
| --- | --- | --- |
| error | [Error](#openapi-definition-Error) | - |

#### Объект `Error`

| Элемент | Тип | Описание |
| --- | --- | --- |
| code | integer | Код состояния HTTP |
| constraintViolations | [ConstraintViolation[]](#openapi-definition-ConstraintViolation) | Список нарушений ограничений |
| message | string | Сообщение об ошибке |

#### Объект `ConstraintViolation`

Список нарушений ограничений

| Элемент | Тип | Описание |
| --- | --- | --- |
| location | string | - |
| message | string | - |
| parameterLocation | string | - Элемент может содержать следующие значения: * `HEADER` * `PATH` * `PAYLOAD_BODY` * `QUERY` |
| path | string | - |

### JSON-модели тела ответа

```
{


"entityIds": [


"string"


],


"managementZoneIds": [


1


]


}
```

```
{


"error": {


"code": 1,


"constraintViolations": [


{


"location": "string",


"message": "string",


"parameterLocation": "HEADER",


"path": "string"


}


],


"message": "string"


}


}
```