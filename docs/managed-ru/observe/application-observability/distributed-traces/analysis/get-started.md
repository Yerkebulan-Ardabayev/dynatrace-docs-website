---
title: Начало работы с распределёнными трассировками
source: https://docs.dynatrace.com/managed/observe/application-observability/distributed-traces/analysis/get-started
scraped: 2026-05-12T11:14:11.970352
---

# Начало работы с распределёнными трассировками

# Начало работы с распределёнными трассировками

* How-to guide
* 6-min read
* Updated on Aug 13, 2024

Distributed Traces Classic предоставляет набор инструментов анализа для получения детальной информации о транзакциях в вашем окружении. Он сочетает видимость на уровне кода, информацию о топологии и метаданные с высочайшим уровнем детализации и точности данных. В этой статье вы ознакомитесь с функциональностью Distributed Traces Classic и тем, как использовать её для анализа запросов в вашем окружении.

## Быстрый старт

Чтобы начать анализ распределённой трассировки

1. Перейдите в **Distributed Traces**.
2. Необязательно: используйте диаграмму **Overview**, чтобы понять количество запросов и время отклика за выбранный временной период.
   Хранение данных

   Диаграмма использует [данные трассировок и запросов](/managed/observe/application-observability/multidimensional-analysis#data-source "Настройте представление многомерного анализа и сохраните его как вычисляемую метрику.") с разными периодами хранения. Для временных периодов, содержащих данные старше 10 дней, можно включить переключатель **Show data retention**, чтобы лучше понять, какие данные доступны за какой период непосредственно на диаграмме.
3. Необязательно: в таблице разверните детали трассировки, которую нужно проанализировать, для просмотра её топологии, атрибутов, информации HTTP и временных показателей.
4. Выберите имя распределённой трассировки для углублённой аналитики.
5. Выберите временной сегмент, чтобы развернуть вкладки с подробностями.

## Обзор

Чтобы настроить представление распределённых трассировок в вашем окружении

1. Перейдите в **Distributed Traces**.

   ![Обзор распределённых трассировок](https://dt-cdn.net/images/distributed-traces-overview-3552-8da636c2c1.png)

   Обзор распределённых трассировок
2. Настройте представление с помощью фильтров. Для фильтрации распределённых трассировок по

   * Методу приёма данных

     + Выберите **PurePaths**, чтобы просмотреть распределённые трассировки PurePath®, захваченные OneAgent.
     + Выберите **Ingested traces**, чтобы просмотреть распределённые трассировки, [инструментированные другими библиотеками](/managed/ingest-from/extend-dynatrace/extend-tracing "Узнайте, как расширить наблюдаемость трассировок в Dynatrace.").
   * Сервису

     Перейдите в **Filter requests** > **Service name** и введите имя сервиса. Обратите внимание, что это представление также доступно через **Services** > **More** (**…**) > **Distributed traces** для соответствующего сервиса.

## Экспорт данных обзора

Вы можете экспортировать данные таблицы обзора распределённых трассировок в файл CSV.

1. Перейдите в **Distributed Traces**.
2. Необязательно: уточните результаты таблицы, изменив настройки представления или воспользовавшись полем поиска над таблицей.

   Таблица отображает до **3 000** наиболее актуальных трассировок, захваченных за выбранный временной период в пределах выбранной зоны управления. В зависимости от временного периода и настроенного представления в списке выделяется наиболее важный узел трассировки.

   Поиск узла

   Чтобы найти конкретный узел трассировки

   1. Перейдите в **Distributed Traces**.
   2. Отфильтруйте таблицу по идентификатору трассировки.
   3. Выберите имя трассировки.
   4. В представлении **Trace** найдите нужный узел.
3. В правом нижнем углу страницы выберите меню **Show export**.

   ![Меню Show export](https://dt-cdn.net/images/show-export-menu-107-2a8a76c9a2.png)

   Меню Show export
4. Выберите один из следующих вариантов.

   | Параметр | Экспортируемые данные | Поля | Количество записей |
   | --- | --- | --- | --- |
   | **Export visible data** | Текущая видимая область таблицы с учётом применённых фильтров | Только видимые поля | До 50 трассировок |
   | **Export table data** | Все данные таблицы, включая трассировки, не отображаемые на текущей странице | Все доступные поля, связанные с распределёнными трассировками | Все 3 000 наиболее актуальных трассировок |

## Анализ отдельной трассировки

Представление анализа распределённой трассировки предоставляет сквозную визуализацию запросов в виде каскадной диаграммы для одной трассировки.

Чтобы перейти к представлению отдельной трассировки

1. Перейдите в **Distributed Traces**.
2. Выберите имя распределённой трассировки в таблице.
3. Необязательно: перейдите на вкладку **Execution breakdown**, чтобы узнать распределение времени трассировки.
4. Просмотрите каскадную диаграмму, чтобы узнать, какие сервисы проходит трассировка и в каком порядке.
5. Выберите запрос, чтобы увидеть последующие взаимодействия.

   Используйте цвета и позиции горизонтальных полос на диаграмме, чтобы определить, какие запросы выполнялись синхронно (параллельно). Они показывают как последовательность, так и время отклика каждого запроса.

   ![Распределённые трассировки — обзор трассировки](https://dt-cdn.net/images/distributed-traces-trace-overview-1398-0bc0c909a4.png)

   Распределённые трассировки — обзор трассировки
6. Выберите временной сегмент для углублённого анализа трассировки на вкладках с подробностями вплоть до уровня кода.
7. Выберите вкладку с подробностями для продолжения анализа.

   Summary

   ![Распределённая трассировка — вкладка Summary](https://dt-cdn.net/images/purepath-distributed-trace-summary-tab-1578-9a1abde976.png)

   Распределённая трассировка — вкладка Summary

   |  |  |
   | --- | --- |
   | Доступность | по умолчанию |
   | Содержимое | Включает данные метаданных, заголовков запроса, параметров запроса и информацию о прокси, через который был отправлен запрос. |

   Значения, скрытые пятью звёздочками (`*****`), означают [скрытые конфиденциальные данные](/managed/manage/data-privacy-and-security/data-privacy/personal-data-captured-by-dynatrace "Узнайте, какие типы данных конечных пользователей могут быть захвачены в процессе мониторинга Dynatrace, и методы маскирования персональных данных."), которые могут быть раскрыты пользователями с соответствующими правами. Три или менее звёздочки (`***`) используются в целях агрегации данных и не могут быть раскрыты.

   Timings

   ![Распределённая трассировка — временные показатели](https://dt-cdn.net/images/purepath-distributed-trace-timings-1581-677870facf.png)

   Распределённая трассировка — временные показатели

   |  |  |
   | --- | --- |
   | Доступность | по умолчанию |
   | Содержимое | Детальные временные показатели сервисов. Выберите **View method hotspots** для получения дополнительной информации о вкладе в общее время (например, CPU, ожидание, синхронизация, блокировка и время выполнения). |
   | Пример | В приведённом примере показаны детали временных показателей для вызова веб-сервиса `/orange.jsf`. В данном случае видно, что запрос занимает **57.7 мс**. |

   Code level

   ![Детализация распределённой трассировки на уровне кода](https://dt-cdn.net/images/purepath-distributed-trace-code-level-insights-1581-dca5962209.png)

   Детализация распределённой трассировки на уровне кода

   |  |  |
   | --- | --- |
   | Доступность | требуется OneAgent; зависит от источника данных [1](#fn-1-1-def) |
   | Содержимое | Детальное выполнение кода для каждого запроса, включая выполнение методов на уровне кода и их временные показатели в точной последовательности событий. |
   | Пример | В приведённом примере Dynatrace точно указывает, какой метод в запросе `/orange.jsf` к сервису `easyTravel Customer Frontend` вызвал соответствующие веб-сервисы и какие именно методы веб-сервиса были вызваны. Отображаемые временные показатели — это время, зафиксированное со стороны `easyTravel Customer Frontend`, которое в случае вызовов к сервисам на удалённых уровнях представляет собой время на стороне клиента. |

   1

   Объём информации на уровне кода варьируется в зависимости от важности, временных показателей и расчётных накладных расходов. Как правило, более медленные части запроса содержат больше деталей.

   Logs

   |  |  |
   | --- | --- |
   | Доступность | требуется обогащение логов; зависит от источника данных [1](#fn-2-1-def) |
   | Содержимое | Записи логов в полном контексте транзакции. По умолчанию включаются все доступные идентификаторы span.  * Чтобы отфильтровать логи, относящиеся к выбранному узлу каскадной диаграммы, включите **Strict span filter**. * Чтобы просмотреть все логи для трассировки, выберите **View logs for this trace**. |
   | Пример | [Использование обогащения логов для трассировок при решении проблем](/managed/observe/application-observability/distributed-traces/use-cases/problems-logs-traces "Используйте обогащение логов для просмотра связанных записей в представлении распределённых трассировок и расширения возможностей анализа.") |

   1

   Для начала работы см. [Обогащение логов](/managed/analyze-explore-automate/log-monitoring/log-monitoring-configuration/log-enrichment "Узнайте, как связать входящие данные логов с трассировками для более точного анализа в Dynatrace.").

   Errors

   |  |  |
   | --- | --- |
   | Доступность | зависит от источника данных |
   | Содержимое | Возникающие исключения для неудачных запросов. |
   | Пример | [Детализация до причин сбоев сервиса](/managed/observe/application-observability/distributed-traces/use-cases/error-analysis "Устранение ошибок с помощью распределённых трассировок.") |

   Threads

   |  |  |
   | --- | --- |
   | Доступность | требуется OneAgent; зависит от типа сервиса |

   Integrations

   |  |  |
   | --- | --- |
   | Доступность | зависит от типа сервиса |

## Часто задаваемые вопросы

Что означает `[declared unavailable by agent]`?

`[declared unavailable by agent]` — это значение-заполнитель, отображаемое в представлении трассировки, когда метаданные недоступны для захвата OneAgent, например из-за отсутствия видимости данных или ограничений в их передаче (например, технология не поддерживается OneAgent). Рекомендуется обратиться к специалисту по продуктам Dynatrace через чат в вашем окружении Dynatrace для разбора конкретной ситуации.

## Связанные темы

* [Сервисы](/managed/observe/application-observability/services-classic "Узнайте о классическом мониторинге сервисов в Dynatrace.")
* [Поток сервисов](/managed/observe/application-observability/services-classic/service-flow "Узнайте, как Dynatrace помогает отслеживать последовательность вызовов сервисов, инициируемых каждым запросом в вашем окружении.")
* [Временные показатели анализа сервисов](/managed/observe/application-observability/services-classic/service-analysis-timing "Узнайте, что означает каждый временной показатель в анализе сервисов.")