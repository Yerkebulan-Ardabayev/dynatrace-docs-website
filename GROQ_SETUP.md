# 🚀 Настройка Groq API (БЕСПЛАТНО!)

## Почему Groq?

- ⚡ **В 10 РАЗ БЫСТРЕЕ** обычных AI (500 токенов/сек vs 50)
- 💰 **100% БЕСПЛАТНО** (30 запросов в минуту)
- 🎯 **Отличное качество** (Llama 3.1 70B = уровень GPT-4)
- 🌍 **Перевод 1000 страниц за 10-15 минут** (вместо 2 часов!)

---

## 📝 Шаг 1: Получить API ключ

### 1.1 Регистрация

1. Перейдите на: **https://console.groq.com**
2. Нажмите **"Sign Up"** (Регистрация)
3. Войдите через:
   - GitHub
   - Google
   - Email

### 1.2 Создание API ключа

1. После входа откройте: **https://console.groq.com/keys**
2. Нажмите **"Create API Key"**
3. Введите название: `Dynatrace Docs`
4. Скопируйте ключ (начинается с `gsk_...`)

⚠️ **ВАЖНО:** Сохраните ключ сразу - больше не увидите!

---

## 🔧 Шаг 2: Установить ключ

### Windows PowerShell:
```powershell
$env:GROQ_API_KEY = "gsk_your_key_here"
```

### Windows CMD:
```cmd
set GROQ_API_KEY=gsk_your_key_here
```

### Linux/Mac:
```bash
export GROQ_API_KEY=gsk_your_key_here
```

### ИЛИ создать файл `.env`:
```env
GROQ_API_KEY=gsk_your_key_here
```

---

## 📋 Шаг 3: Обновить скрипты

### 3.1 Перевод документации

В файле `scripts/translate_docs_groq.py` замените строку 12:

```python
API_KEY = os.environ.get('GROQ_API_KEY', 'gsk_demo_key_placeholder')
```

На:

```python
API_KEY = os.environ.get('GROQ_API_KEY', 'gsk_ВАШ_КЛЮЧ_ЗДЕСЬ')
```

### 3.2 AI чат в браузере

В файле `docs/assets/javascripts/groq-chat.js` замените строку 7:

```javascript
const GROQ_API_KEY = 'gsk_demo_key_placeholder';
```

На:

```javascript
const GROQ_API_KEY = 'gsk_ВАШ_КЛЮЧ_ЗДЕСЬ';
```

---

## ✅ Шаг 4: Проверка

Запустите тестовый перевод:

```bash
cd scripts
python translate_docs_groq.py
```

Вы должны увидеть:
```
🚀 СУПЕР-БЫСТРЫЙ ПЕРЕВОД DYNATRACE ДОКУМЕНТАЦИИ
🤖 Модель: Groq Llama 3.1 70B
⚡ Скорость: В 10 РАЗ БЫСТРЕЕ обычного!
```

---

## 📊 Лимиты бесплатного тира

| Параметр | Значение |
|----------|----------|
| **Запросов в минуту** | 30 |
| **Запросов в день** | 14,400 |
| **Токенов в минуту** | 6,000 |
| **Стоимость** | **БЕСПЛАТНО!** 🎉 |

**Этого хватит для:**
- ✅ Перевод 1000+ страниц документации
- ✅ Сотни вопросов в AI чате
- ✅ Ежедневные автообновления

---

## 🎯 Использование

### Перевод документации:
```bash
python scripts/translate_docs_groq.py
```

### AI чат:
Откройте сайт и нажмите кнопку **"AI ⚡"** в правом нижнем углу!

### Автообновление:
```bash
python auto_update.py
```

---

## 💡 Советы

### 1. Безопасность ключа
- ❌ **НЕ** коммитьте ключ в Git
- ✅ Используйте переменные окружения
- ✅ Добавьте `.env` в `.gitignore`

### 2. Оптимизация
- Groq автоматически использует кеширование
- Повторные переводы берутся из кеша (мгновенно!)

### 3. Мониторинг
Следите за использованием: https://console.groq.com/settings/limits

---

## 🆚 Сравнение с Gemini

| Параметр | Gemini 1.5 Pro | Groq Llama 3.1 70B |
|----------|----------------|-------------------|
| **Скорость** | ~50 токенов/сек | ⚡ **~500 токенов/сек** |
| **Качество** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **Лимит (бесплатно)** | 2 запроса/мин | **30 запросов/мин** |
| **Время перевода 1000 стр** | ~2 часа | ⚡ **10-15 минут** |

**Вывод:** Groq быстрее и щедрее на лимиты!

---

## ❓ Проблемы?

### Ошибка: "Invalid API Key"
✅ Проверьте, что ключ начинается с `gsk_`
✅ Убедитесь, что нет пробелов в начале/конце

### Ошибка: "Rate limit exceeded"
✅ Подождите 1 минуту (лимит 30 запросов/мин)
✅ Скрипт автоматически делает паузы

### Ошибка: "API Key not found"
✅ Установите переменную окружения `GROQ_API_KEY`
✅ Или замените в коде напрямую

---

## 🎉 Готово!

Теперь у вас:
- ⚡ Супер-быстрый AI чат (Groq)
- 🚀 Молниеносный перевод документации
- 💰 Всё это БЕСПЛАТНО!

**Наслаждайтесь скоростью Groq!** 🚀
